server:
  port: 8081

spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  application:
    name: api-passenger
  redis:
    port: 6379
    database: 0
    host: localhost
